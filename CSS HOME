/* Variable Definitions */
:root {
    --primary-color: #007bff; /* Set your primary color */
    --link-color: var(--primary-color); /* Set your link color */
    --link-hover-color: #0056b3; /* Set your link hover color */
    --navbar-bg: #fff; /* Set your navbar background color */
    --navbar-text-color: #333; /* Set your navbar text color */
    --navbar-toggler-color: #333; /* Set your navbar toggler color */
    --masthead-bg: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('your-background-image.jpg') center/cover no-repeat; /* Set your masthead background image and overlay styles */
    --masthead-text-color: #fff; /* Set your masthead text color */
    --body-bg: #f8f9fa; /* Set your body background color */
    --footer-bg: #000; /* Set your footer background color */
    --btn-primary-bg: var(--primary-color); /* Set your primary button background color */
    --btn-primary-color: #fff; /* Set your primary button text color */
    --btn-primary-hover-bg: #0056b3; /* Set your primary button hover background color */
}

/* Reset some default styles */
body,
h1,
h2,
h3,
p {
    margin: 0;
    padding: 0;
}

body {
    font-family: 'Nunito', sans-serif;
    font-size: 16px;
    line-height: 1.6;
    color: #333;
    background-color: var(--body-bg);
}

a {
    color: var(--link-color);
    text-decoration: none;
}

a:hover {
    color: var(--link-hover-color);
}

.container {
    max-width: 1200px;
    margin-right: auto;
    margin-left: auto;
}

/* Navigation styles */
.navbar {
    background-color: var(--navbar-bg);
}

.navbar-brand,
.navbar-nav a {
    color: var(--navbar-text-color);
}

.navbar-toggler {
    border-color: var(--navbar-toggler-color);
}

/* Masthead styles */
.masthead {
    height: 100vh;
    background: var(--masthead-bg);
    color: var(--masthead-text-color);
    text-align: center;
    display: flex;
    align-items: center;
    justify-content: center;
}

.masthead h1 {
    font-size: 3rem;
}

.masthead h2 {
    font-size: 1.5rem;
}

/* About section styles */
.about-section {
    padding: 80px 0;
}

/* Projects section styles */
.projects-section {
    padding: 80px 0;
}

/* Signup section styles */
.signup-section {
    padding: 80px 0;
}

/* Footer styles */
.footer {
    padding: 20px 0;
    background-color: var(--footer-bg);
}

/* Button styles */
.btn {
    display: inline-block;
    padding: 10px 20px;
    font-size: 1rem;
    text-align: center;
    text-transform: uppercase;
    border: none;
    border-radius: 3px;
    cursor: pointer;
}

.btn-primary {
    background-color: var(--btn-primary-bg);
    color: var(--btn-primary-color);
}

.btn-primary:hover {
    background-color: var(--btn-primary-hover-bg);
}
